# Removed any real API keys from repository. Add your Hugging Face token below.
# Get a free token at https://huggingface.co/settings/tokens
# Example (do NOT commit real tokens):
# HF_API_TOKEN=hf_xxxYourTokenHerexxx
HF_API_TOKEN=

# Optional: set a default HF model to use for suggestions. Common small models: "gpt2" or community models.
# HF_MODEL=gpt2

# If you still want to use OpenAI for now, set OPENAI_API_KEY in your environment (not committed here).
